{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "auth-config",
  "description": "Better Auth configuration setup",
  "dependencies": [
    "better-auth"
  ],
  "files": [
    {
      "path": "src/registry/default/lib/auth.ts",
      "content": "import { betterAuth } from \"better-auth\";\nimport { drizzleAdapter } from \"better-auth/adapters/drizzle\";\n// import { db } from \"@/db\"; // Import your Drizzle database instance\n\n// Placeholder for database if not available immediately\nconst db: any = {};\n\nexport const auth = betterAuth({\n  database: drizzleAdapter(db, {\n    provider: \"pg\", // Options: \"pg\", \"mysql\", \"sqlite\"\n  }),\n  // Email & Password Support\n  emailAndPassword: {\n    enabled: true,\n  },\n  // Social Providers\n  socialProviders: {\n    // github: {\n    //   clientId: process.env.GITHUB_CLIENT_ID!,\n    //   clientSecret: process.env.GITHUB_CLIENT_SECRET!,\n    // },\n    // google: {\n    //   clientId: process.env.GOOGLE_CLIENT_ID!,\n    //   clientSecret: process.env.GOOGLE_CLIENT_SECRET!,\n    // },\n  },\n});\n",
      "type": "registry:lib",
      "target": "lib/auth.ts"
    }
  ],
  "type": "registry:lib"
}